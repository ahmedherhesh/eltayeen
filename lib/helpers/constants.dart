import 'package:flutter/material.dart';

String appTitle = 'التعيين';
const TextStyle textStyle = TextStyle(fontSize: 16, fontFamily: 'Cairo');
const Color primaryColor = Color(0xff2f3542);
String date = "${DateTime.now()}".split(' ')[0];
const double padding = 20, margin = 20;
const lettersCount = 3;
